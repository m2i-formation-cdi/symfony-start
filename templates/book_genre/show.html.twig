{% extends 'base.html.twig' %}

{% block title %}BookGenre{% endblock %}

{% block body %}
    <h1>BookGenre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ book_genre.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ book_genre.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('book_genre_index') }}">back to list</a>

    <a href="{{ path('book_genre_edit', {'id': book_genre.id}) }}">edit</a>

    {{ include('book_genre/_delete_form.html.twig') }}
{% endblock %}
